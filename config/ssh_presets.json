{
  "presets": [
    {
      "id": "spark_main",
      "name": "Spark-кластер (основной)",
      "command": "ssh -L 8080:localhost:8080 user@spark-host -p 22",
      "remote_api": "http://localhost:8080/v1/chat/completions",
      "tunnel_port": 8080
    },
    {
      "id": "spark_test",
      "name": "Spark (тестовый)",
      "command": "ssh -L 8081:localhost:8081 user@test-host -p 2222",
      "remote_api": "http://localhost:8081/v1/chat/completions",
      "tunnel_port": 8081
    }
  ]
}